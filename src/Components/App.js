import React from "react";
import CreateTimerModal from "./CreateTimerModal";
import MainPage from "./MainPage";

const App = () => (
  <div>
    <MainPage />
    <CreateTimerModal />
  </div>
);

export default App;
